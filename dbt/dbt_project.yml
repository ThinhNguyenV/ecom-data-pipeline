name: 'ecom_dbt'
version: '1.0.0'
config-version: 2

profile: 'ecom_dev'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  ecom_dbt:
    materialized: view
    staging:
      +materialized: table
    marts:
      +materialized: table
