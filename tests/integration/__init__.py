"""
Integration tests package.

Integration tests require real services (Kafka, PostgreSQL, Elasticsearch).
Run with: pytest tests/integration/ -v --integration
Configure services via docker-compose before running.
"""
